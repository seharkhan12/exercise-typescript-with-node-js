"use strict";
let personName = ;
personName = propmt('what is your name?') || '';
if (personName !== null && personName !== '') {
    alert(Hello, $, { personName }, would, you, like, to, learn, some, phython, today ? ' )
        :
    , {});
}
